/**
 * PostCSS pipeline used by the frontend build.
 * Autoprefixer is built into @tailwindcss/postcss in Tailwind v4.
 */
export default {
    plugins: {
        "@tailwindcss/postcss": {},
    },
}
